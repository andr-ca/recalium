---
// website/src/components/Nav.astro
const links = [
  { href: '/', label: 'Home' },
  { href: '/features', label: 'Features' },
  { href: '/pricing', label: 'Pricing' },
  { href: '/docs', label: 'Docs' },
];
const currentPath = Astro.url.pathname;
---

<header
  id="nav"
  class="fixed top-0 left-0 right-0 z-50 border-b border-transparent transition-all duration-200 nav-transparent"
>
  <!-- Skip to content link moved to BaseLayout.astro -->

  <nav class="max-w-6xl mx-auto px-6 h-14 flex items-center gap-8" aria-label="Main navigation">
    <!-- Logo -->
    <a href="/" class="gradient-text font-extrabold text-base tracking-tight shrink-0">
      Recalium
    </a>

    <!-- Center links (desktop) -->
    <ul class="hidden md:flex gap-6 flex-1 justify-center list-none" role="list">
      {links.map(({ href, label }) => (
        <li>
          <a
            href={href}
            class={`text-sm transition-colors ${
              currentPath === href
                ? 'text-text-secondary'
                : 'text-text-muted hover:text-text-secondary'
            }`}
            aria-current={currentPath === href ? 'page' : undefined}
          >
            {label}
          </a>
        </li>
      ))}
    </ul>

    <!-- Right side -->
    <div class="hidden md:flex items-center gap-3 shrink-0">
      <a
        href="https://github.com/recalium/recalium"
        target="_blank"
        rel="noopener noreferrer"
        class="text-sm text-text-muted hover:text-text-secondary border border-border rounded-full px-4 py-1.5 transition-colors"
      >
        <span aria-hidden="true">★</span> GitHub
      </a>
      <a
        href="/docs"
        class="text-sm font-semibold bg-accent text-white rounded-full px-4 py-1.5 hover:bg-indigo-500 transition-colors focus-visible:ring-2 focus-visible:ring-accent focus-visible:outline-none"
      >
        Get Started
      </a>
    </div>

    <!-- Hamburger (mobile) -->
    <button
      id="nav-toggle"
      class="md:hidden ml-auto text-text-muted hover:text-text-secondary p-2"
      aria-label="Toggle menu"
      aria-expanded="false"
      aria-controls="mobile-menu"
    >
      <svg width="20" height="20" viewBox="0 0 20 20" fill="currentColor">
        <rect y="3" width="20" height="2" rx="1"/>
        <rect y="9" width="20" height="2" rx="1"/>
        <rect y="15" width="20" height="2" rx="1"/>
      </svg>
    </button>
  </nav>

  <!-- Mobile menu -->
  <div
    id="mobile-menu"
    class="hidden md:hidden bg-page border-t border-border px-6 py-4"
  >
    <ul class="flex flex-col gap-4 list-none" role="list">
      {links.map(({ href, label }) => (
        <li>
          <a
            href={href}
            class="text-sm text-text-secondary"
            aria-current={currentPath === href ? 'page' : undefined}
          >
            {label}
          </a>
        </li>
      ))}
      <li>
        <a
          href="https://github.com/recalium/recalium"
          target="_blank"
          rel="noopener noreferrer"
          class="text-sm text-text-muted"
        >
          <span aria-hidden="true">★</span> GitHub
        </a>
      </li>
      <li>
        <a
          href="/docs"
          class="text-sm font-semibold bg-accent text-white rounded-full px-4 py-2 inline-block focus-visible:ring-2 focus-visible:ring-accent focus-visible:outline-none"
        >
          Get Started
        </a>
      </li>
    </ul>
  </div>
</header>

<script>
  // Scroll-aware nav: add frosted glass effect after scrolling past hero
  const nav = document.getElementById('nav');
  const toggle = document.getElementById('nav-toggle');
  const menu = document.getElementById('mobile-menu');

  function updateNav() {
    if (window.scrollY > 20) {
      nav?.classList.add('nav-scrolled');
      nav?.classList.remove('nav-transparent');
      nav?.classList.add('border-border');
      nav?.classList.remove('border-transparent');
    } else {
      nav?.classList.remove('nav-scrolled');
      nav?.classList.add('nav-transparent');
      nav?.classList.remove('border-border');
      nav?.classList.add('border-transparent');
    }
  }

  window.addEventListener('scroll', updateNav, { passive: true });
  updateNav();

  toggle?.addEventListener('click', () => {
    const expanded = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!expanded));
    menu?.classList.toggle('hidden');
  });
</script>
