---
phase: 05-service-hardening
plan: "06"
type: verify
wave: 4
depends_on: ["05-05"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - INGT-04
  - INGT-05
  - MCP-02
  - PORT-01
must_haves:
  truths:
    - "All Phase 5 integration tests pass GREEN"
    - "All prior tests still pass (no regressions, 172+ passing)"
    - "ROADMAP.md marks Phase 5 complete"
    - "STATE.md updated with final test count and Phase 5 completion"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 marked complete"
    - path: ".planning/STATE.md"
      provides: "Updated progress and test count"
---

<objective>
This plan runs all tests to confirm Phase 5 integration tests are GREEN and no regressions exist, then updates the roadmap and state files to mark Phase 5 complete.
</objective>

<execution_context>
@~/.agents/skills/superpowers/executing-plans/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
**Key contracts this plan must respect:**

1. Run tests with: `cd /home/andrey/projects/recalium/backend && uv run python3 -m pytest -q --tb=short`
2. Target: all tests pass (Phase 5 tests + 172 prior tests)
3. ROADMAP.md update: mark Phase 5 plans complete in the Plans section, update status table
4. STATE.md update: update stopped_at, last_updated, progress, phase transition log
</interfaces>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Run full test suite and verify GREEN</name>
  <files></files>
  <action>
  Run the complete test suite:

  ```bash
  cd /home/andrey/projects/recalium/backend && uv run python3 -m pytest -q --tb=short 2>&1
  ```

  Then run Phase 5 tests specifically for detailed output:

  ```bash
  cd /home/andrey/projects/recalium/backend && uv run python3 -m pytest tests/integration/test_phase5_integration.py -v --tb=short 2>&1
  ```

  **If any tests fail:** Fix the failures before proceeding. Common issues:
  - Import errors in test file → fix the `pytest.importorskip` guards or module paths
  - `_tool_manager` attribute not available → check the FastMCP API for listing tools (may be `mcp_app.tools` or another attribute; adapt the test accordingly)
  - Session commit conflicts in watcher tests → ensure the factory session is committed before checking DB
  - Missing `processed/` dir in watcher tests → the `_ingest_file` function creates it, but `tmp_path` must be fresh

  Do NOT update ROADMAP.md or STATE.md until tests are GREEN.
  </action>
  <verify>
    <automated>cd /home/andrey/projects/recalium/backend && uv run python3 -m pytest -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass (0 failures)
    - Phase 5 tests are GREEN
    - 172+ tests total passing
  </acceptance_criteria>
  <done>Full test suite passes with Phase 5 tests GREEN.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP.md — mark Phase 5 complete</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>.planning/ROADMAP.md</read_first>
  <action>
  Update `.planning/ROADMAP.md`:

  1. Change the Phase 5 status line from:
  ```
  - [ ] **Phase 5: Service Hardening** — MCP ingest, watched folder, portability bundle, API/MCP contract hardening
  ```
  to:
  ```
  - [x] **Phase 5: Service Hardening** — MCP ingest, watched folder, portability bundle, API/MCP contract hardening (completed 2026-03-24)
  ```

  2. In the Phase 5 details section, change `**Plans:** TBD` to:
  ```
  **Plans:** 6/6 plans complete

  Plans:
  - [x] 05-01-PLAN.md — Watched import folder service + settings (INGT-04)
  - [x] 05-02-PLAN.md — MCP ingest tool (MCP-02, INGT-05)
  - [x] 05-03-PLAN.md — Portability bundle export/import + API versioning (PORT-01)
  - [x] 05-04-PLAN.md — Service boundary comments (PORT-01)
  - [x] 05-05-PLAN.md — Phase 5 integration test suite + db_session_phase5 fixture
  - [x] 05-06-PLAN.md — Phase 5 integration tests GREEN + roadmap/state update
  ```

  3. Update the progress table: change Phase 5 row from `0/TBD | Not started | —` to:
  ```
  | 5. Service Hardening | 6/6 | Complete | 2026-03-24 |
  ```

  4. Update the last-updated footer line at the bottom to:
  ```
  *Last updated: 2026-03-24 after completing Phase 5 (all 6 plans executed, [N] tests passing, 4 Phase 5 reqs verified)*
  ```
  (Replace `[N]` with the actual test count from Task 1.)
  </action>
  <verify>
    <automated>cd /home/andrey/projects/recalium && grep "Phase 5" .planning/ROADMAP.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md has Phase 5 marked `[x]`
    - Progress table shows Phase 5 as Complete
    - Plans list shows all 6 plans complete
  </acceptance_criteria>
  <done>ROADMAP.md updated to mark Phase 5 complete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md — final state</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/STATE.md</read_first>
  <action>
  Update `.planning/STATE.md`:

  1. Update the YAML front matter:
  ```yaml
  status: complete
  stopped_at: "Completed 05-06 — Phase 5 integration tests GREEN ([N] tests passing)"
  last_updated: "2026-03-24T00:00:00.000Z"
  progress:
    total_phases: 5
    completed_phases: 5
    total_plans: 38
    completed_plans: 31
  ```
  (Replace `[N]` with actual test count.)

  2. Update the "Current Focus" line in the Project Reference section from `Phase 04` to `Phase 05 — Service Hardening (complete)`.

  3. Update the Phase list in Current Position:
  ```
  Phase: 05 (Service Hardening) — COMPLETE ✓ ([N] tests passing)
  ```

  4. Update the progress bar to show Phase 5 at 100%:
  ```
  Phase 5 [Service Hardening   ] ████████████████████  100% (6/6 plans complete)

  Overall ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓  52/52 requirements complete (all phases verified)
  ```

  5. Update Requirements complete in Performance Metrics: `52/52`.

  6. Add Phase 5 completion entry to the Phase Transition Log table:
  ```
  | Phase 5 complete | 2026-03-24 | All 6 plans executed; [N] total tests green; 4 reqs verified (INGT-04, INGT-05, MCP-02, PORT-01) |
  ```

  7. Update the Resume Point at the bottom:
  ```
  **Stopped at:** Phase 5 complete — all 5 phases done, [N] tests passing
  **Resume file:** N/A — milestone complete
  **Next step:** Lifecycle audit → complete → cleanup
  ```

  8. Update the last-updated footer.
  </action>
  <verify>
    <automated>cd /home/andrey/projects/recalium && grep "status:" .planning/STATE.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md shows `status: complete`
    - Phase 5 marked complete in Current Position
    - Progress shows 5/5 phases complete
    - 52/52 requirements complete
  </acceptance_criteria>
  <done>STATE.md updated to reflect Phase 5 completion and milestone status.</done>
</task>
</tasks>

<verification>
```bash
# Final verification: all tests pass
cd /home/andrey/projects/recalium/backend && uv run python3 -m pytest -q --tb=short 2>&1 | tail -5

# Verify Phase 5 specifically
cd /home/andrey/projects/recalium/backend && uv run python3 -m pytest tests/integration/test_phase5_integration.py -v 2>&1 | tail -20

# Confirm ROADMAP and STATE updated
grep "Phase 5" /home/andrey/projects/recalium/.planning/ROADMAP.md | head -3
grep "status:" /home/andrey/projects/recalium/.planning/STATE.md | head -3
```
</verification>

<success_criteria>
- All tests pass (0 failures, 0 errors)
- Phase 5 integration tests are GREEN
- ROADMAP.md marks Phase 5 `[x]` complete
- STATE.md shows `status: complete` and 5/5 phases done
- 52/52 requirements tracked as verified
</success_criteria>

<output>
After completion, the Recalium v1.0 milestone is fully executed across all 5 phases. Next step: lifecycle audit → complete → cleanup.
</output>
