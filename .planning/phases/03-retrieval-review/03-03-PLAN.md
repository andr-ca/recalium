---
phase: 03-retrieval-review
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - backend/app/domain/retrieval/__init__.py
  - backend/app/domain/retrieval/service.py
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06
  - MCP-01

must_haves:
  truths:
    - "retrieve() function accepts RetrievalRequest and returns RetrievalResponse"
    - "keyword mode queries fts_entries.search_vector using websearch_to_tsquery"
    - "semantic mode queries embeddings table using pgvector cosine similarity"
    - "hybrid mode merges top-50 per mode using RRF k=60, returns top-20"
    - "RRF min threshold 1/(60+25) filters out low-quality results"
    - "Budget trimming follows canonical→facts→summaries→raw order strictly"
    - "Budget trimming never truncates a mid-item — skips item if it doesn't fit"
    - "hybrid with no embeddings sets degraded_mode=True and falls back to keyword"
    - "Response envelope matches architecture spec: query, retrieval_mode, budget_used, budget_limit, trimming_reason, items[]"
    - "Each item carries: id, type, content, score, source_id, source_system, captured_at, conflict_label, provenance{}"
    - "LRU cache (256 entries, 60s TTL) caches retrieval results"
    - "All queries filter source_status='active' and deleted_at IS NULL"
    - "Audit event emitted for every retrieve() call"
    - "test_retrieval.py goes GREEN"
---

<objective>
Implement the core retrieval service at backend/app/domain/retrieval/service.py. This is the most critical piece of Phase 3 — it implements keyword search (PostgreSQL FTS), semantic search (pgvector), hybrid RRF merge, strict priority budget trimming, degraded-mode fallback, in-process LRU caching, and audit event emission.
</objective>

<execution_context>
@~/.agents/skills/superpowers/executing-plans/SKILL.md
</execution_context>

<context>
@.planning/phases/03-retrieval-review/03-CONTEXT.md
@docs/architecture/retrieval-and-ranking.md
@backend/app/domain/derived_memory/models.py
@backend/app/domain/archive/models.py
@backend/app/domain/audit/models.py
@backend/tests/domain/test_retrieval.py
</context>

<interfaces>
From retrieval-and-ranking.md:
- RRF: k=60, top-50 per mode, top-20 merged. Min threshold: 1/(60+25) ≈ 0.01176.
- Budget trimming order: canonical → facts → summaries → raw excerpts. Stop when budget met. Never truncate mid-item.
- Cache: 256 entries, 60s TTL, key=hash(query+filters+mode+budget).
- Response envelope: query, retrieval_mode, budget_used, budget_limit, trimming_reason ("budget_met"|"result_exhausted"), items[].
- Each item: id, type, content, score, source_id, source_system, captured_at, conflict_label, provenance{derivation_method, derivation_model, source_excerpt}.

Audit event (from audit-and-provenance.md):
- event_type: "search" for UI queries, "mcp_retrieve" for MCP
- actor: "user_ui" or MCP client identity
- operation_metadata: {query_summary, result_count, retrieval_mode, policy_decision}

Priority layer type mapping:
- "canonical" — from canonical_memory table
- "fact" — from facts table
- "summary" — from summaries table
- "excerpt" — from raw_archive (raw_content snippet)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests first, then implement retrieval service</name>
  <files>
    backend/app/domain/retrieval/__init__.py
    backend/app/domain/retrieval/service.py
  </files>
  <read_first>
    - backend/tests/domain/test_retrieval.py (the tests to make GREEN)
    - backend/app/domain/derived_memory/models.py (FtsEntry, Embedding, Fact, Summary models)
    - backend/app/domain/canonical_memory/models.py (CanonicalMemoryItem model)
    - backend/app/domain/audit/models.py (AuditEvent model)
    - docs/architecture/retrieval-and-ranking.md (exact algorithm spec)
  </read_first>
  <action>
1. Run tests to confirm RED: `cd backend && python -m pytest tests/domain/test_retrieval.py -q 2>&1`

2. Create `backend/app/domain/retrieval/__init__.py`:
```python
"""Retrieval domain — keyword, semantic, and hybrid search with context budgeting."""
```

3. Create `backend/app/domain/retrieval/service.py`:

```python
"""Retrieval service — keyword, semantic, and hybrid search.

Architecture spec: docs/architecture/retrieval-and-ranking.md
- Keyword: PostgreSQL FTS via websearch_to_tsquery('english', query)
- Semantic: pgvector cosine similarity on embeddings table
- Hybrid: RRF merge (k=60, top-50 per mode, top-20 merged)
- Budget trimming: canonical → facts → summaries → raw (strict priority; no mid-item truncation)
- LRU cache: 256 entries, 60s TTL
- Audit event emitted for every retrieve() call

SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05, SRCH-06, MCP-01, MCP-03
"""
from __future__ import annotations

import hashlib
import json
import logging
import uuid
from dataclasses import dataclass, field
from typing import Literal

from cachetools import TTLCache
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from app.domain.audit.models import AuditEvent

logger = logging.getLogger(__name__)

# ── Constants ─────────────────────────────────────────────────────────────────
RRF_K: int = 60
RRF_CANDIDATES_PER_MODE: int = 50
RRF_FINAL_TOP_N: int = 20
RRF_MIN_THRESHOLD: float = 1 / (RRF_K + 25)  # ≈ 0.01176
DEFAULT_BUDGET: int = 2000

# ── In-process LRU cache ──────────────────────────────────────────────────────
# Key: hash(query + filters + mode + budget). TTL: 60s. Max: 256 entries.
# MUST be invalidated on new index publications and deletion events.
_cache: TTLCache = TTLCache(maxsize=256, ttl=60)


# ── Request/Response models ───────────────────────────────────────────────────
@dataclass
class RetrievalFilters:
    category: str | None = None
    source_system: str | None = None
    time_range_start: str | None = None
    time_range_end: str | None = None
    canonical_only: bool = False


@dataclass
class RetrievalRequest:
    query: str
    mode: Literal["keyword", "semantic", "hybrid"] = "hybrid"
    budget: int = DEFAULT_BUDGET
    filters: RetrievalFilters = field(default_factory=RetrievalFilters)
    actor: str = "user_ui"
    limit: int = RRF_FINAL_TOP_N


@dataclass
class RetrievalItem:
    id: str
    type: Literal["canonical", "fact", "summary", "excerpt"]
    content: str
    score: float
    source_id: str
    source_system: str
    captured_at: str
    conflict_label: str | None
    provenance: dict


@dataclass
class RetrievalResponse:
    query: str
    retrieval_mode: str
    budget_used: int
    budget_limit: int
    trimming_reason: Literal["budget_met", "result_exhausted"]
    items: list[RetrievalItem]
    degraded_mode: bool = False


# ── Cache helpers ─────────────────────────────────────────────────────────────

def _cache_key(req: RetrievalRequest) -> str:
    payload = json.dumps({
        "q": req.query,
        "mode": req.mode,
        "budget": req.budget,
        "filters": {
            "category": req.filters.category,
            "source_system": req.filters.source_system,
            "time_range_start": req.filters.time_range_start,
            "time_range_end": req.filters.time_range_end,
            "canonical_only": req.filters.canonical_only,
        },
    }, sort_keys=True)
    return hashlib.sha256(payload.encode()).hexdigest()


def invalidate_cache() -> None:
    """Invalidate the entire retrieval cache.

    Call after new FTS/embedding publications or deletion events.
    For v1, full invalidation is acceptable. Partial invalidation is future optimization.
    """
    _cache.clear()
    logger.debug("Retrieval cache invalidated")


# ── RRF helpers ───────────────────────────────────────────────────────────────

def rrf_score(rank: int, k: int = RRF_K) -> float:
    """Reciprocal Rank Fusion score. rank is 1-based."""
    return 1.0 / (k + rank)


def _merge_rrf(
    keyword_ids: list[str],
    semantic_ids: list[str],
) -> list[tuple[str, float]]:
    """Merge two ranked lists using RRF. Returns (id, rrf_score) sorted desc."""
    scores: dict[str, float] = {}

    for rank, item_id in enumerate(keyword_ids, start=1):
        scores[item_id] = scores.get(item_id, 0.0) + rrf_score(rank)

    for rank, item_id in enumerate(semantic_ids, start=1):
        scores[item_id] = scores.get(item_id, 0.0) + rrf_score(rank)

    # Filter by minimum threshold
    filtered = {k: v for k, v in scores.items() if v >= RRF_MIN_THRESHOLD}

    # Sort by score descending, take top N
    return sorted(filtered.items(), key=lambda x: x[1], reverse=True)[:RRF_FINAL_TOP_N]


# ── Budget trimming ───────────────────────────────────────────────────────────

# Priority order for budget trimming (SRCH-04)
_PRIORITY_ORDER = ["canonical", "fact", "summary", "excerpt"]


def apply_budget_trimming(
    items: list[RetrievalItem],
    budget: int,
) -> tuple[list[RetrievalItem], int, Literal["budget_met", "result_exhausted"]]:
    """Apply strict priority budget trimming.

    Order: canonical → fact → summary → excerpt.
    Never truncate mid-item. Skip item if it doesn't fit.
    Returns (trimmed_items, budget_used, trimming_reason).
    """
    # Sort by priority layer first, then score within layer
    priority_map = {t: i for i, t in enumerate(_PRIORITY_ORDER)}
    sorted_items = sorted(items, key=lambda x: (priority_map.get(x.type, 99), -x.score))

    result: list[RetrievalItem] = []
    used = 0

    for item in sorted_items:
        item_len = len(item.content)
        if used + item_len <= budget:
            result.append(item)
            used += item_len
        # If item doesn't fit: skip entirely (never truncate)
        if used >= budget:
            return result, used, "budget_met"

    return result, used, "result_exhausted"


# ── Candidate retrieval ───────────────────────────────────────────────────────

async def _keyword_candidates(
    session: AsyncSession,
    query: str,
    limit: int = RRF_CANDIDATES_PER_MODE,
) -> list[dict]:
    """Retrieve keyword candidates using PostgreSQL FTS.

    Queries: fts_entries (for summaries+facts text), canonical_memory (generated FTS),
    and raw_archive content (fallback).

    Returns list of {id, type, content, score, source_id, source_system, captured_at,
                      conflict_label, provenance} dicts.
    """
    # FTS on fts_entries (covers summaries + facts indexed text)
    fts_result = await session.execute(
        text("""
            SELECT
                fe.id::text AS id,
                'excerpt' AS type,
                LEFT(fe.text_content, 500) AS content,
                ts_rank_cd(fe.search_vector, websearch_to_tsquery('english', :q)) AS score,
                fe.raw_archive_id::text AS source_id,
                COALESCE(ra.source_type, 'unknown') AS source_system,
                ra.ingested_at::text AS captured_at,
                NULL::text AS conflict_label,
                fe.raw_archive_id::text AS archive_id
            FROM fts_entries fe
            JOIN raw_archive ra ON ra.id = fe.raw_archive_id
            WHERE fe.source_status = 'active'
              AND ra.deleted_at IS NULL
              AND fe.search_vector @@ websearch_to_tsquery('english', :q)
            ORDER BY score DESC
            LIMIT :limit
        """),
        {"q": query, "limit": limit},
    )
    fts_rows = fts_result.mappings().all()

    # FTS on canonical_memory
    canon_result = await session.execute(
        text("""
            SELECT
                cm.id::text AS id,
                'canonical' AS type,
                cm.content AS content,
                ts_rank_cd(cm.search_vector, websearch_to_tsquery('english', :q)) AS score,
                COALESCE(cm.raw_archive_id::text, cm.id::text) AS source_id,
                'canonical' AS source_system,
                cm.created_at::text AS captured_at,
                NULL::text AS conflict_label,
                cm.raw_archive_id::text AS archive_id
            FROM canonical_memory cm
            WHERE cm.source_status = 'active'
              AND cm.status = 'active'
              AND cm.search_vector @@ websearch_to_tsquery('english', :q)
            ORDER BY score DESC
            LIMIT :limit
        """),
        {"q": query, "limit": limit},
    )
    canon_rows = canon_result.mappings().all()

    candidates = []
    for row in list(canon_rows) + list(fts_rows):
        candidates.append({
            "id": row["id"],
            "type": row["type"],
            "content": row["content"] or "",
            "score": float(row["score"] or 0),
            "source_id": row["source_id"] or "",
            "source_system": row["source_system"] or "unknown",
            "captured_at": str(row["captured_at"] or ""),
            "conflict_label": None,
            "provenance": {
                "derivation_method": "fts_retrieval",
                "derivation_model": "postgresql_fts",
                "source_excerpt": (row["content"] or "")[:200],
            },
        })

    return candidates[:limit]


async def _semantic_candidates(
    session: AsyncSession,
    query: str,
    limit: int = RRF_CANDIDATES_PER_MODE,
) -> tuple[list[dict], bool]:
    """Retrieve semantic candidates using pgvector cosine similarity.

    Returns (candidates, degraded) where degraded=True if no embeddings exist.
    Requires embed_text() from derived_memory.service — if sentence-transformers
    not installed, falls back to empty pool (degraded mode).
    """
    try:
        from app.domain.derived_memory.service import embed_text
        query_vector = await embed_text(query)
    except RuntimeError:
        # sentence-transformers not available — degraded mode
        logger.info("Semantic search unavailable: sentence-transformers not installed (degraded mode)")
        return [], True

    result = await session.execute(
        text("""
            SELECT
                e.id::text AS id,
                e.raw_archive_id::text AS raw_archive_id,
                1 - (e.embedding <=> :vec) AS score,
                ra.source_type AS source_system,
                ra.ingested_at::text AS captured_at
            FROM embeddings e
            JOIN raw_archive ra ON ra.id = e.raw_archive_id
            WHERE e.source_status = 'active'
              AND ra.deleted_at IS NULL
              AND e.embedding_model = :model
            ORDER BY e.embedding <=> :vec
            LIMIT :limit
        """),
        {
            "vec": str(query_vector),
            "model": "all-MiniLM-L6-v2",
            "limit": limit,
        },
    )
    rows = result.mappings().all()

    if not rows:
        return [], True  # No embeddings — degraded

    # Fetch content for each hit (summary text preferred, fall back to raw_content[:500])
    candidates = []
    for row in rows:
        # Get summary text if available, otherwise raw excerpt
        summary_result = await session.execute(
            text("""
                SELECT summary_text FROM summaries
                WHERE raw_archive_id = :aid AND source_status = 'active'
                LIMIT 1
            """),
            {"aid": row["raw_archive_id"]},
        )
        summary_row = summary_result.fetchone()

        if summary_row:
            content = summary_row[0]
            item_type = "summary"
        else:
            raw_result = await session.execute(
                text("SELECT LEFT(raw_content, 500) FROM raw_archive WHERE id = :aid"),
                {"aid": row["raw_archive_id"]},
            )
            raw_row = raw_result.fetchone()
            content = raw_row[0] if raw_row else ""
            item_type = "excerpt"

        candidates.append({
            "id": row["id"],
            "type": item_type,
            "content": content or "",
            "score": float(row["score"] or 0),
            "source_id": row["raw_archive_id"],
            "source_system": row["source_system"] or "unknown",
            "captured_at": str(row["captured_at"] or ""),
            "conflict_label": None,
            "provenance": {
                "derivation_method": "semantic_retrieval",
                "derivation_model": "all-MiniLM-L6-v2",
                "source_excerpt": (content or "")[:200],
            },
        })

    return candidates, False


# ── Main retrieve function ────────────────────────────────────────────────────

async def retrieve(
    session: AsyncSession,
    req: RetrievalRequest,
) -> RetrievalResponse:
    """Execute retrieval and return a context-budgeted response.

    Modes: keyword, semantic, hybrid.
    Always emits an AuditEvent.
    Uses in-process LRU cache for repeated identical queries.
    """
    cache_key = _cache_key(req)
    if cache_key in _cache:
        logger.debug("Retrieval cache hit for query=%r", req.query[:50])
        return _cache[cache_key]

    degraded = False
    effective_mode = req.mode

    # ── Gather candidates ─────────────────────────────────────────────────
    if req.mode == "keyword":
        candidates = await _keyword_candidates(session, req.query, RRF_CANDIDATES_PER_MODE)

    elif req.mode == "semantic":
        candidates, degraded = await _semantic_candidates(session, req.query, RRF_CANDIDATES_PER_MODE)

    else:  # hybrid
        kw_candidates = await _keyword_candidates(session, req.query, RRF_CANDIDATES_PER_MODE)
        sem_candidates, sem_degraded = await _semantic_candidates(session, req.query, RRF_CANDIDATES_PER_MODE)

        if sem_degraded or not sem_candidates:
            # Degraded: fall back to keyword only
            degraded = True
            effective_mode = "keyword"
            candidates = kw_candidates
        else:
            # RRF merge
            kw_ids = [c["id"] for c in kw_candidates]
            sem_ids = [c["id"] for c in sem_candidates]
            merged_scored = _merge_rrf(kw_ids, sem_ids)

            # Build id → candidate lookup
            all_by_id: dict[str, dict] = {c["id"]: c for c in kw_candidates + sem_candidates}
            candidates = []
            for item_id, rrf_s in merged_scored:
                if item_id in all_by_id:
                    c = dict(all_by_id[item_id])
                    c["score"] = rrf_s  # Override with RRF score
                    candidates.append(c)

    # ── Build RetrievalItem list ──────────────────────────────────────────
    items = [
        RetrievalItem(
            id=c["id"],
            type=c["type"],
            content=c["content"],
            score=c["score"],
            source_id=c["source_id"],
            source_system=c["source_system"],
            captured_at=c["captured_at"],
            conflict_label=c.get("conflict_label"),
            provenance=c.get("provenance", {}),
        )
        for c in candidates
    ]

    # ── Budget trimming ───────────────────────────────────────────────────
    trimmed_items, budget_used, trimming_reason = apply_budget_trimming(items, req.budget)

    response = RetrievalResponse(
        query=req.query,
        retrieval_mode=effective_mode,
        budget_used=budget_used,
        budget_limit=req.budget,
        trimming_reason=trimming_reason,
        items=trimmed_items,
        degraded_mode=degraded,
    )

    # ── Audit event ───────────────────────────────────────────────────────
    audit_event = AuditEvent(
        event_type="search" if req.actor == "user_ui" else "mcp_retrieve",
        actor=req.actor,
        operation_metadata={
            "query_summary": req.query[:100],
            "result_count": len(trimmed_items),
            "retrieval_mode": effective_mode,
            "degraded_mode": degraded,
            "policy_decision": "allowed",
        },
    )
    session.add(audit_event)
    await session.commit()

    # ── Cache result ──────────────────────────────────────────────────────
    _cache[cache_key] = response
    return response
```

4. Add `cachetools` to `backend/pyproject.toml` dependencies:
```
"cachetools>=5.0,<6",
```

5. Install cachetools:
```bash
cd backend && uv pip install "cachetools>=5.0,<6"
```
  </action>
  <verify>
    <automated>cd /home/andrey/projects/recalium/backend && python -m pytest tests/domain/test_retrieval.py -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - All tests in test_retrieval.py pass (GREEN)
    - `rrf_score(rank=1, k=60)` returns approximately 1/61 ≈ 0.01639
    - `RRF_MIN_THRESHOLD` equals `1 / (60 + 25)`
    - `apply_budget_trimming` respects canonical→fact→summary→excerpt order
    - `apply_budget_trimming` with budget smaller than item size returns empty list
    - `RetrievalResponse` has all required fields: query, retrieval_mode, budget_used, budget_limit, trimming_reason, items, degraded_mode
    - `RetrievalItem` has all required fields: id, type, content, score, source_id, source_system, captured_at, conflict_label, provenance
    - Service imports without error
  </acceptance_criteria>
  <done>Retrieval service implemented. test_retrieval.py GREEN.</done>
</task>

</tasks>

<verification>
After task completion:
1. `pytest tests/domain/test_retrieval.py -v 2>&1` — all tests PASS
2. `python -c "from app.domain.retrieval.service import retrieve, RetrievalRequest, RetrievalResponse, rrf_score, RRF_MIN_THRESHOLD, apply_budget_trimming; print('imports OK')"` — exits 0
3. `python -c "from app.domain.retrieval.service import rrf_score, RRF_MIN_THRESHOLD; import math; assert abs(rrf_score(1,60) - 1/61) < 1e-6; assert abs(RRF_MIN_THRESHOLD - 1/85) < 1e-6; print('RRF constants OK')"` — exits 0
</verification>

<success_criteria>
- test_retrieval.py fully GREEN
- Retrieval service handles all three modes: keyword, semantic, hybrid
- Budget trimming is strictly ordered and never truncates mid-item
- Degraded mode properly detected and flagged
- Audit events emitted for every retrieve() call
- LRU cache (cachetools TTLCache) used for repeated queries
</success_criteria>

<output>
After completion, create `.planning/phases/03-retrieval-review/03-03-SUMMARY.md`
</output>
