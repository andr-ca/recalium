# Quick Task 260707-tt8: Gate observability (F15) + calibration (F22)

**Mode:** quick (executed inline by orchestrator; empirical calibration requires live-stack iteration)
**Created:** 2026-07-08

## Findings driving this plan (measured in-container)

- The NLI CrossEncoder is unusable: even with per-pair softmax and improved
  hypotheses, "PostgreSQL indexing" → personal-profile P(ent)=0.93; the truly
  personal conversation does not stand out. No calibration rescues it.
- Embedding-prototype classification with all-MiniLM-L6-v2 (already required
  for embeddings) separates cleanly: tech convs → general at 0.42–0.75 sim
  with ≥0.26 margin; personal/health convs → personal_profile 0.47–0.72.

## Tasks

1. **Gate calibration (F22)** — replace NLI pass with embedding-prototype
   classification in `backend/app/domain/policy/gate.py`:
   - Keyword heuristics pass unchanged (fast block path)
   - Pass 2: cosine similarity of text embedding vs per-category prototype
     sentences; decision rule (privacy-first): sensitive category blocks at
     sim ≥ 0.25; `general` allows only at sim ≥ 0.35 AND margin ≥ 0.15;
     otherwise unclassified→blocked (PRIV-05 default-block unchanged)
   - Decision rule as a pure function (unit-testable without the model); TDD
2. **Gate observability (F15)** — dispatcher emits
   `AuditEvent(event_type="sensitivity_gate", raw_archive_id, operation_metadata={category, confidence, blocked, method})`
   after classification; add `raw_archive_id` to the audit API response
3. **Eval integration** — sensitivity check verifies the gate EXACTLY via
   audit events (blocked=true for sensitive items, blocked=false for controls),
   keeping the differential fact check as a second signal
4. **Validation** — rebuild+restart; `make eval` with Ollama: extraction and
   sensitivity checks must go live (no longer skipped) and pass; update
   recommendations doc (F15/F22 status) + evidence artifacts

## Verify

- `pytest tests/domain/test_policy_gate.py tests/worker -q` green
- `make eval`: 5/5 non-skipped checks passing, extraction producing facts
